#!/bin/sh

# set custom keyboard binding (e.g. fn + key)
#
# xbindkeys package need to be installed. You can also install xbindkeys-config
# if you want a gui program to set new keybinding.
xbindkeys

# invoke global X session script
. /etc/X11/Xsession

